// ENG 
the prototyped functioning of the app is perfect. Original JSON database is included for adding other custom question or the ones from the database. Database is hosted on aws RDS server. Access must be granted to get into the database, for info, contact me. 
The database has three tables. There's login functionality integrated in the database. There's also integrated grading database with post functionality. Last feature is custom overall grading with the average value of all grading areas. Last feature has some flaws due
to some round errors of the app (w.i.p feature). The app is serverless and access to the database is granted trough vanilla jdbc driver. GUI is made with javaFX and compiled with maven and open jDK 25.
// ITA
il prototipo dell' app è stato testato e perfezionato. E' incluso il json originale con il set di domande per esserne aggiunte di nuove o riprendendo alcuna da quel file. Il database è hostato su AWS. L'accesso al database deve essere autorizzato. 
Per informazioni, contattatemi. Il database ha 3 tabelle. C'è la funzionalità di login integrata. C'è anche un'algoritmo di graduatoria con inserimento dei livelli di certificaizone all'interno del database. 
L'ultima funzionalità è l'algoritmo di certificazione generale, basato sulla media di tutti i livelli.
La seguente funzionalità ha ancora degli errori di arrotondamento (funzionalità in fase di sviluppo). L'applicazione non richiede collegamento a dei server e l'accesso al database avviene tramite driver originale integrato di java (jdbc). L'inferfaccia è fatta con 
javaFX ed openJDK 25.

